;;; user-helm --- contains user configuration for Helm-Mode
;;; Commentary:
;;; Code:

(require 'helm)
(require 'helm-config)
(helm-mode 1)
(helm-M-x 1)

(provide 'user-helm)

;;; user-helm.el ends here
